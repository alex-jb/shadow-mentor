# V10 device validation result (fill on the device day)

- date / operator:
- device model / android / abi / battery / storage:
- glasses model / Eye attached / firmware / MyGlasses version:
- adb devices state (must be 'device'):
- APK installed (sha256 must match reports/device-v10/V10_CORE_APK_SHA256.txt):

## Per-stage outcome (paste logs / samples; tick only with evidence)
- [ ] STAGE A app+loader — loader reached STARTED? log excerpt:
- [ ] STAGE B 3DoF — pose samples:
- [ ] STAGE C input — action results:
- [ ] STAGE D Eye+6DoF — tracking mode + translation deltas (both required):
- [ ] STAGE E device TTS — engine/voice + latencies:
- [ ] STAGE F RGB frame (camera candidate) — non-black frame + hash:
- [ ] STAGE G OCR — provider + seal + verify result:
- [ ] STAGE 14 performance — per-condition numbers:

## STOP condition hit? which / fallback used:

## Honest statuses reached (only with device evidence):
