#!/usr/bin/env bash
# Placeholder: the XREAL candidate is a separate, SDK-gated build (SHADOW_XREAL_SDK). It does not
# exist until the XREAL SDK is imported + that candidate is built. This script fails loudly rather
# than installing the base candidate in its place.
echo "XREAL candidate not built yet. It requires the XREAL SDK import + a gated build."; exit 1
