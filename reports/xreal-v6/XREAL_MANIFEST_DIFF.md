# XREAL manifest diff — PENDING IMPORT
Diff the merged AndroidManifest of the XREAL candidate vs the base candidate. Record every new
<uses-permission>, <uses-feature>, <activity>, <service>, <provider>, and XR loader entry the SDK adds.
The base candidate (offline) must stay unchanged; XREAL permissions live only in the XREAL candidate.
