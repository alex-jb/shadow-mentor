# Deferred recommendations (valid, scheduled for later V11 increments)

Ordered by the task's P0→P2. Each is confirmed-real but intentionally not done in this reconciliation pass;
they are the next increments of the V11 product-convergence work.

## P0 design (next)
- Make the **guided-story surface consume `XREAL_OST_BRIGHT`** (dark-on-bright plates) — the images prove it
  fails on bright backgrounds. Highest-value visual fix.
- **Audit Workspace** as the primary Unity product UI (card/rail/focus), primitives demoted to diagnostic.
- **Connect tracking-health → presentation** (auto safe-3DoF/2D fallback on loss); Scanning as an explicit
  user-facing state.

## P1 quality
- CORS origin allowlist documentation + no-credentials-with-wildcard guard.
- Ambient Council layout repair (§14). Audit Room inspector/hover/help/flat-framing (§13).
- CJK wrapping via `Intl.Segmenter` (§13G). Edge-merge / instancing perf (measured, §18).
- anchors.js DER fuzz + algorithm allowlist.

## P2 future
- 2D replay `?bundle=` safe preload; dist single-file; spatial-finance treemap labels; SELF_SIGNED
  disclosure in a NEW versioned verifier candidate (never the frozen one); browser SBS dual-format prep.

## Device-day (Beam Pro)
- SBS squeeze vs frame-packed confirmation; OST readability A/B; 3DoF/6DoF/controller/TTS/camera validation.
  All remain **NOT device-validated** until hardware arrives.
